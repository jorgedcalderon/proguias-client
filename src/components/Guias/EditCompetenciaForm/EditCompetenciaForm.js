import React from "react";

import "./EditCompetenciaForm.scss";

export default function EditCompetenciaForm(props) {
    const { guia } = props;

    return (
        <h4>Agrega los documentos de las competencias con las que cuentes:</h4>
    );
}